DATABASE_URL="postgresql://postgres:password@localhost:5432/figmashop?schema=public"

# ⚠️  CHANGE THIS before deploying — generate with: openssl rand -hex 32
JWT_SECRET="figmashop-secret-key-change-in-production-2024-replace-with-64-char-random"

PORT=3001

# Comma-separated list of allowed frontend origins
FRONTEND_URL="http://localhost:5173,http://localhost:5174"

# Email (SMTP) — leave blank to log emails to console in development
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="ChilaloShop <noreply@chilaloshop.com>"
